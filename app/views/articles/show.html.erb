<h2><%= @article.title %></h2>

<div>
  <%= link_to edit_path(@article) do %><i class="far fa-edit"></i><% end %>

  <%= link_to article_path(@article), method: :delete, data: { confirm: 'Are you sure?' } do %>
    <i class="far fa-trash-alt"></i>
  <% end %>
</div>

<p><%= @article.content%></p>

<%= link_to "Back to home page", articles_path%>
